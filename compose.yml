services:
  dev:
    build: .
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
    environment:
      - ASTRO_TELEMETRY_DISABLED=1
